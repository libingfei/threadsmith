# Anchor PM 产品原则

## 1. 产品定位

Anchor PM 是 Codex-first 的 AI coding 项目协作锚点框架。

它的核心任务不是替用户开发项目，而是把长期 AI 协作中的线程边界、共享状态、交接流程和复盘机制固化成一套轻量协议。

一句话定位：

> Anchor PM turns scattered AI coding chats into anchored specialist threads.

中文定位：

> Anchor PM 把分散的 AI 编程对话，收束成一组可重锚、可交接、可维护的专业线程。

## 2. Codex-first, not Codex-only

第一平台以 Codex 为准。

优先支持：

- Codex package-first 安装
- Codex Skill
- Codex Plugin
- Codex Desktop 的本地技能发现
- Codex 风格的线程重锚、交接和状态回写

但核心协议不绑定 Codex 私有 UI。

核心锚点必须保持为：

- 普通 Markdown
- 普通目录结构
- 可版本控制
- 可 diff
- 可手工阅读
- 可由 CLI 复现

这不是为了现在同时适配多个平台，而是为了避免把产品价值锁死在某一个 UI 缺口上。

1.0 主路径是 Codex package-first：通过包内安装提示词、安装计划、模板和 workflow，让 Codex 完成审计、计划和经确认后的锚点部署。CLI 是后续复现、检查和批量化工具，不是 1.0 的前置依赖。

## 3. 中心层轻，模块层深

Anchor PM 不做超级代理。

它只维护：

- 项目边界
- 线程职责
- 状态结构
- 版本重锚
- 跨线程交接
- 文档收口
- 复发性问题记录

技术细节留给模块线程。

人类仍是：

- 需求提供者
- 行业专家
- 跨模块裁决者
- 上线和范围升级的最终决策者

## 4. 默认不污染项目

首次运行默认生成安装计划，而不是直接写入项目。

只有用户显式确认后，才写入项目目录。

正式写入必须满足：

- 有 diff
- 可回滚
- 不覆盖用户已有规则
- 不自动删除文件
- 不静默改业务代码

安装产物必须让用户少猜。

尤其是线程初始化提示词：输出给用户的 `thread_initialization.md` 必须提供每个线程可直接粘贴的完整提示词。占位符只能存在于包内部模板和生成说明中，不能作为用户最终需要填写的内容。

交互语言也必须让用户舒服。项目文档可以保持英文，但 Codex 线程回复用户时应跟随用户惯用语言，不因为 Anchor PM 包文档是英文就强制英文交流。

## 5. 少概念，少文档，强边界

Anchor PM 第一版只允许少量核心概念：

- Project Constitution
- Thread Contract
- Module State
- Reanchor
- Handoff
- Review Log
- Status Check

禁止第一版引入：

- RAG
- 向量数据库
- 任务管理系统
- 自动调度平台
- 多后端平台适配
- 常驻自治 agent
- 大而全模板市场

## 6. 锚点优先，不做默认 RAG

Anchor PM 的记忆机制是可审计锚点，不是自由检索。

默认上下文恢复顺序：

1. 项目宪法
2. 契约版本
3. 当前线程契约
4. 当前模块状态
5. 必要时定向查看 runbook 或参考材料

RAG 如未来存在，只能作为外部 evidence provider。

Anchor PM 可以导出文档 metadata，但不内置 embedding、召回排序或语义记忆。

## 7. 自动生成草案，不自动发明真相

自动审计结果必须分层：

- `Observed`：文件、脚本、配置中直接看到
- `Inferred`：工具推断
- `Needs Confirmation`：必须用户确认

未确认的推断不能进入正式契约。

工具不能把“看起来像模块”直接写成项目事实。

## 8. 状态文件是长期记忆，不是第二套大脑

模块状态文件只保存：

- 当前状态
- 未决问题
- 运行入口
- 历史备注
- 少量长期风险

不保存：

- 全量聊天记录
- 所有分析细节
- 大量一次性报告
- 业务实现长解释

## 9. 越界必须交接，不静默扩展

模块线程只处理本模块。

跨模块时：

- 不继续硬做
- 不伪装成自己懂全局
- 输出交接摘要
- 保留对当前线程的影响

Anchor PM 只维护交接格式，不替目标线程做判断。

## 10. 强结论必须可审计

强结论包括：

- pass
- consistent
- ready
- safe
- can ship
- no issue

这些结论必须能追到：

- 正式脚本
- 正式报告
- 检查产物
- 阻断项与非阻断风险

否则只能说：

```text
Current evidence supports it, but it is not fully closed yet.
```

## 11. 好产品的判断标准

Anchor PM 不是靠功能数量取胜。

核心目标是做一个真的能提高用户 AI coding 体验的工具，而不是维护一套自洽但无用的流程。

成功标准：

- 10 分钟内生成可审阅草案
- 线程边界一眼能看懂
- 重锚提示稳定可用
- 状态文件数量受控
- 不制造新文档山
- 不让中心层成为瓶颈
- 用户能明显减少重复解释和跨线程纠错

失败信号：

- 用户需要读很多手册才会用
- 生成的文档比原项目还复杂
- 所有任务都必须经过 Anchor PM
- 插件开始替业务线程做业务判断
- 工具输出用户不信任的“项目真相”
- 真实用户在安装、线程创建、重锚或 handoff 中持续卡住
- 用户没有明显减少重复解释、上下文纠错或跨线程同步成本

开发过程中一旦发现用户难以理解、使用成本超过收益，或现有方法论无法真实改善 AI coding 体验，必须及时反馈并重新评估；如果价值不成立，应中止无意义工作，而不是继续扩展框架。

## 12. 先自迭代

Anchor PM 必须优先服务自己的开发过程。

这不是把本项目变成特殊案例，而是用最小真实项目持续验证：

- 线程契约是否真的能减少重复解释
- 状态文件是否足够短
- 重锚是否足够快
- 交接摘要是否能减少串线
- 文档治理是否能阻止规则膨胀

自迭代约束：

- 本项目自己的锚点结构必须保持普通 Markdown。
- 本项目的自迭代过程由 Coordination 线程统一管理，避免为小项目制造额外线程复杂度。
- 本项目不能依赖尚未实现的 CLI 才能继续协作。
- 1.0 self-evolution 包只能生成下一版本优化建议，不能自动修改产品文档或源码。
- 手工维护的自举锚点必须能在后续 CLI 实现后被工具检查或复现。
- 自迭代中发现的问题必须先进入 review log 或模块状态，不直接膨胀成新规则。

## 13. 第一版口号

Make AI coding threads boring, bounded, and recoverable.

中文：

> 让 AI 编程线程变得边界清楚、状态可恢复、协作不串线。
