# Product Manager Aliases

Layer: `3`

Thread: `Product Manager`

Aliases:

- `Layer 0` - framework baseline.
- `Layer 1` - thread definition.
- `Layer 2` - cross-thread shared state.
- `Layer 3` - thread local memory.
- `Thread Management` - owner flow for installation, thread creation, renaming,
  removal, and definition updates.
